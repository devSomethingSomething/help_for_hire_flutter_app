///Imports
import 'package:flutter/material.dart';

/// Custom Large spacer widget
class LargeSpacerWidget extends StatelessWidget {
  /// Condstructor
  const LargeSpacerWidget();

  /// Builds the widget
  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 48.0,
    );
  }
}
