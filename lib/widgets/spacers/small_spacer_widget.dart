/// Imports
import 'package:flutter/material.dart';

/// Custom Small spacer widget
class SmallSpacerWidget extends StatelessWidget {
  /// Condstructor
  const SmallSpacerWidget();

  /// Builds the widget
  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 16.0,
    );
  }
}
