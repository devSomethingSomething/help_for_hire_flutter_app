import 'package:flutter/material.dart';

class SmallSpacerWidget extends StatelessWidget {
  const SmallSpacerWidget();

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 16,
    );
  }
}
