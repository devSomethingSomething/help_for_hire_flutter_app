/// Imports
import 'package:flutter/material.dart';

/// Custom Medium spacer widget
class MediumSpacerWidget extends StatelessWidget {
  /// Condstructor
  const MediumSpacerWidget();

  /// Builds the widget
  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 32.0,
    );
  }
}
