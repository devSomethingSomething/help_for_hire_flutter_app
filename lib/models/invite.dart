class Invite {
  int inviteId;
  String status;
  // Worker worker;
  // Employer employer;

  Invite({
    required this.inviteId,
    required this.status,
    //required this.worker,
    //required this.employer,
  });
}
