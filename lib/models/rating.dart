class Rating{
  int ratingId;
  int value;
  String description;
  //Worker worker;
  //Employer employer;

  Rating({
    required this.ratingId,
    required this.value,
    required this.description,
    //required this.worker,
    //required this.employer,
  });
}